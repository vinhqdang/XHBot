\documentclass[../elsevier.tex]{subfiles}
\begin{document}

\section{Numerical Validation}
\label{sec:validation}

Every closed form in Section~\ref{sec:theory} is checked against Monte-Carlo
simulation of Definition~\ref{def:ccsbm}. Our purpose here is verification of the
analysis, not a performance claim: the results below establish that the derivations
are correct, and say nothing on their own about accuracy on real bot graphs.
Section~\ref{sec:limitations} addresses that gap directly.

\paragraph{Protocol.}
Unless stated otherwise we use $\pi_B = 0.3$ ($c \approx 0.4286$),
$\|\Delta\| = 2$, $\sigma = 1$, and $D = 16$, drawing $4\times 10^5$ nodes per class
per configuration ($1.5 \times 10^6$ for the crossover sweep). Errors are computed
by exhaustive threshold search over the empirical distributions, so the reported
figures are attained minima rather than plug-in estimates.
Appendix~\ref{app:repro} records the remaining numerical choices.

\subsection{Deflation and error}

Table~\ref{tab:deflation} compares Proposition~\ref{prop:deflation} and
Theorem~\ref{thm:error} against simulation. Separation matches to within
$1.8\times 10^{-3}$ and the standard deviation---including the mixture term that a
naive calculation omits---to within $8\times 10^{-4}$ across the whole range.
Figure~\ref{fig:deflation} shows the same agreement graphically.

\begin{table}[t]
\centering
\caption{Theory versus simulation for signal deflation and balanced error
($\pi_B = 0.3$, $\|\Delta\|=2$, $\sigma=1$, $D=16$).}
\label{tab:deflation}
\small
\resizebox{\columnwidth}{!}{%
\begin{tabular}{@{}ccccccc@{}}
\toprule
& & \multicolumn{2}{c}{separation $\delta$} & \multicolumn{2}{c}{$s_{\mathrm{B}}$} & error \\
\cmidrule(lr){3-4}\cmidrule(lr){5-6}
$\rho$ & $\eta$ & theory & sim. & theory & sim. & thy / sim. \\
\midrule
0.00 & 0.000 & 2.0000 & 2.0000 & 0.2500 & 0.2499 & 0.00003 / 0.00003 \\
0.10 & 0.043 & 1.7143 & 1.7142 & 0.2915 & 0.2918 & 0.00113 / 0.00160 \\
0.20 & 0.086 & 1.4286 & 1.4283 & 0.3202 & 0.3204 & 0.00925 / 0.01095 \\
0.30 & 0.129 & 1.1429 & 1.1441 & 0.3391 & 0.3391 & 0.03698 / 0.03895 \\
0.40 & 0.171 & 0.8571 & 0.8586 & 0.3500 & 0.3499 & 0.09785 / 0.09898 \\
0.50 & 0.214 & 0.5714 & 0.5696 & 0.3536 & 0.3528 & 0.19898 / 0.19974 \\
0.60 & 0.257 & 0.2857 & 0.2864 & 0.3500 & 0.3500 & 0.33734 / 0.33680 \\
\bottomrule
\end{tabular}%
}
\end{table}

\begin{figure}[t]
    \centering
    \includegraphics[width=\linewidth]{figures/theory_deflation.png}
    \caption{Theory versus simulation. (a) Post-aggregation separation decays
    linearly in $\rho$ exactly as Proposition~\ref{prop:deflation} predicts.
    (b) Balanced error against the attribute-only baseline; the predicted critical ratio
    $\rho^\star = 0.464$ coincides with the crossing point.}
    \label{fig:deflation}
\end{figure}

\subsection{The critical ratio}

The central prediction is Corollary~\ref{cor:critical}. Solving
$\mathrm{SNR}(\rho) = \|\Delta\|/(2\sigma)$ numerically at $D=16$ gives
$\rho^\star = 0.4644$. Sweeping $\rho$ in steps of $0.01$ with $1.5\times 10^6$
samples per point, aggregation beats raw features at $\rho = 0.46$
($\varepsilon = 0.1545$ against $\varepsilon_{\mathrm{raw}} = 0.1587$) and loses at
$\rho = 0.47$ ($\varepsilon = 0.1648$). The measured crossover therefore lies in
$[0.46, 0.47]$, containing the predicted value.

Table~\ref{tab:critical} separates the two forms of the corollary. The exact
crossover differs from the closed form~\eqref{eq:critical} at moderate SNR, as
expected, since~\eqref{eq:critical} drops the mixture variance. Re-running the
exact solver in the noise-dominated regime it was derived for ($\|\Delta\| = 0.2$,
$\sigma = 1$) recovers the closed form to within $1.2\times10^{-3}$ at every degree,
confirming that the discrepancy is the stated approximation and not an error.

\begin{table}[t]
\centering
\caption{Critical camouflage ratio: closed form~\eqref{eq:critical} against the
exact SNR crossover, at moderate and low SNR.}
\label{tab:critical}
\small
\begin{tabular}{@{}cccc@{}}
\toprule
$D$ & closed form~\eqref{eq:critical} & exact ($\|\Delta\|/\sigma = 2$) & exact (low SNR) \\
\midrule
4  & 0.3500 & 0.2604 & 0.3488 \\
9  & 0.4667 & 0.3913 & 0.4657 \\
16 & 0.5250 & 0.4644 & 0.5243 \\
25 & 0.5600 & 0.5102 & 0.5594 \\
64 & 0.6125 & 0.5807 & 0.6121 \\
\bottomrule
\end{tabular}
\end{table}

Figure~\ref{fig:critical} plots both curves against degree. The tolerated
camouflage rises steeply for sparsely connected accounts and flattens as $D$ grows,
which has a concrete operational reading: the accounts most vulnerable to
camouflage are those with few connections, and a deployment that aggregates over
small neighbourhoods is fragile in exactly the regime where bots are cheapest to
create.

\begin{figure}[t]
    \centering
    \includegraphics[width=0.72\linewidth]{figures/theory_critical_ratio.png}
    \caption{Critical camouflage ratio against neighbourhood size. Below the lower
    curve, averaging over alters improves on attributes alone; between the curves it is
    actively harmful; above the upper edge $(1+D^{-1/2})/(1+c)$ the neighbourhood is
    strongly anti-correlated with the label and informative again with the orientation
    reversed. The closed form is the noise-dominated limit of the exact crossover, and
    the upper edge exceeds $1$---so that no reversal regime exists---whenever
    $c < D^{-1/2}$.}
    \label{fig:critical}
\end{figure}

\subsection{The tie-scoring rule}

Lemma~\ref{lem:energy} predicts expected squared discrepancies of $2d\sigma^2$ and
$\|\Delta\|^2 + 2d\sigma^2$. At $d = 8$, $\sigma = 1$, $\|\Delta\| = 2$ these are
$16$ and $20$; simulation over $2\times10^5$ edge samples gives $16.005$ and
$20.005$, an error below $0.03\%$.

For Theorem~\ref{thm:sgtr} we verify the closed
form~\eqref{eq:rhostarsgtr} by round-trip: substituting
$\rho^\star_{\mathrm{prune}}$ back into $\rho_{\mathrm{eff}}$ must return
$\rho^\star$. The maximum absolute deviation over
$\bar e_c \in \{0.25, 0.5, 0.75, 0.9\}$ is $1.1\times 10^{-16}$, i.e.\ machine
precision. Table~\ref{tab:sgtr} reports the resulting tolerance gains, and
Figure~\ref{fig:sgtr} plots the full curve.

\begin{table}[t]
\centering
\caption{Effect of the spectral rule attenuation on tolerated camouflage under the
composition-only form~\eqref{eq:rhostarsgtr} ($D=16$, $\pi_B=0.3$, baseline
$\rho^\star = 0.525$). These values ignore both the effective-degree cost and the
correct $\eta_{\mathrm{eff}}$; Section~\ref{sec:validation-ext} gives the figures
we stand behind. The table is retained only to show the size of the two corrections.}
\label{tab:sgtr}
\small
\begin{tabular}{@{}ccc@{}}
\toprule
$\bar e_c$ & $\rho_{\mathrm{eff}}$ at $\rho = 0.4$ & $\rho^\star_{\mathrm{prune}}$ \\
\midrule
0.00 & 0.4000 & 0.5250 \\
0.25 & 0.3333 & 0.5957 \\
0.50 & 0.2500 & 0.6885 \\
0.75 & 0.1429 & 0.8155 \\
0.90 & 0.0625 & 0.9170 \\
\bottomrule
\end{tabular}
\end{table}

\begin{figure}[t]
    \centering
    \includegraphics[width=0.78\linewidth]{figures/theory_pruning_tolerance.png}
    \caption{Tolerated camouflage against the attenuation applied to camouflage ties.
    The solid curve is the corrected analysis (Proposition~\ref{prop:deff}, with
    $\eta_{\mathrm{eff}}$ as in~\eqref{eq:pruned-condition}); the dotted curve is the
    composition-only form~\eqref{eq:rhostarsgtr}. The correction is \emph{upward} over
    the plotted range: treating the majority side correctly matters more than the
    effective-degree penalty. The curve is truncated at $\bar e_c = 0.9$ deliberately.
    Equation~\eqref{eq:deff} replaces the random retained count by its mean, which is
    defensible while many ties survive but is exactly the substitution
    Remark~\ref{rem:no-ceiling} shows to be invalid as $\bar e_c \to 1$; that limit is
    governed instead by Proposition~\ref{cor:ceiling}, which says perfect pruning never
    loses. The dash-dotted line marks where half of the \emph{strategic actors} still
    retain enough ties to benefit; majority actors are barely affected.}
    \label{fig:sgtr}
\end{figure}

\subsection{The extensions: achievable pruning and depth}
\label{sec:validation-ext}

The effective-degree formula~\eqref{eq:deff} matches Monte-Carlo variance to
within $0.5\%$ for $\theta \geq 0.5$. At $\theta = 0$ it is off by $7.3\%$
($8.00$ predicted against $7.41$ measured at $\rho=0.5$, $D=16$), and the cause is
not sampling: it is Jensen's inequality on $\mathbb{E}[1/K]$ for the random retained
count $K$, exactly the effect quantified in Remark~\ref{rem:no-ceiling}. Under
perfect pruning the variance-equivalent degree is $1.29$ rather than the $1.00$ that
$\mathbb{E}[K]$ predicts at $\rho = 1-1/D$, $D=16$, and $35.6\%$ of nodes retain no
neighbour at all; the proportion still benefiting falls to one half at
$\rho = 0.897$ ($D=16$) and $0.974$ ($D=64$).

Correcting $\eta_{\mathrm{eff}}$ as in~\eqref{eq:pruned-condition} raises the
tolerances relative to the $c\,\rho_{\mathrm{eff}}$ substitution: at $D=16$,
$\pi_B=0.3$ we obtain $0.609$, $0.715$, $0.847$ and $0.928$ for
$\bar e_c = 0.25, 0.5, 0.75, 0.9$, against $0.594$, $0.679$, $0.788$ and $0.869$
under the incorrect form. The error was therefore pessimistic, and by up to six
percentage points.

For depth we evaluate~\eqref{eq:depth-exact} directly, iterating the
distance recursion for the walk profile and summing the weighted contributions. The weight profile $\sum_j (q^{(L)}_j)^2/N_j$ exceeds the
non-backtracking value $D^{-L}$ by factors of $1.94$, $4.63$, $12.3$ and $35.0$ at
$L = 2,\dots,5$, which is the quantitative size of the term
that~\eqref{eq:backtrack} restores. The resulting crossovers are
$\rho^\star_1 = 0.5250$, $\rho^\star_2 = 0.5868$, $\rho^\star_3 = 0.5528$,
$\rho^\star_4 = 0.5756$ and $\rho^\star_5 = 0.5567$: every multi-layer value lies
above the single-layer one, so depth increases the tolerated camouflage, and the
crossover is \emph{not} depth-independent.

\subsection{Measuring the camouflage ratio on real graphs}
\label{sec:measuring}

The theory is parameterised by $\rho$, which has so far been a modelling device. We
now measure it. TwiBot-20 and TwiBot-22 require author approval for access, which we
did not have. We therefore use \textbf{MGTAB}~\citeyearpar{shi2023mgtab}, an openly
distributed Twitter dataset of $10{,}199$ expert-annotated accounts
($2{,}748$ automated, $7{,}451$ genuine; $c = 0.369$) in which \emph{every} account
carries a label, so that all ties are between labelled actors and the measurement is
not diluted by unlabelled alters. MGTAB records seven relations on the same
population, which lets us separate the social layer (following, friendship, mention,
reply, quote) from a content-coordination layer (shared URLs, shared hashtags). We also use the
two review networks of Dou et al.~\citeyearpar{dou2020caregnn} as a contrast case,
reported in the prose below rather than in Table~\ref{tab:real}; these are not social
ties and we draw no structural conclusion from them, but they carry the attribute
information needed to evaluate the tie-scoring bound of
Section~\ref{sec:achievable}, which MGTAB's inflated attribute separation does not
support.

\paragraph{A negative result on the canonical benchmark.}
It is worth recording why we do not use cresci-2015 or cresci-2017, the
longest-standing bot benchmarks. In the openly distributed preprocessed release,
cresci-2017 contains \emph{no} interpersonal ties at all: all $6{,}637{,}615$ of its
edges are account-to-tweet postings. cresci-2015 does contain following and
friendship ties, but only $0.41\%$ of the $1.29$ million accounts they span are
labelled, and the labelled-to-labelled subgraph reduces to $4{,}275$ ties with the
mixing pattern $3{,}823$ genuine--genuine, $373$ automated--automated and just
$\mathbf{79}$ cross-class. Only $698$ of $3{,}351$ automated accounts ($20.8\%$) have
any labelled tie, the median such account has degree $0$, and the median camouflage
ratio is therefore $0$. A quantity resting on $79$ ties cannot support a
distributional claim, and we obtained the same counts from a second, independently
preprocessed release of the same data, so this is a property of the dataset rather
than of one pipeline. We report it because it explains a gap in the literature: the
benchmark most used for bot detection cannot be used to measure the structural
mechanism the field attributes to bots.

For each relation we compute $\rho$ as the mean over automated accounts of the
fraction of their alters that are genuine, and $\eta$ symmetrically.
Table~\ref{tab:real} reports the result.

\begin{table}[t]
\centering
\small
\caption{Camouflage measured on MGTAB ($10{,}199$ Twitter accounts, all labelled;
$c = 0.369$). $\hat\rho$ is the mean genuine share of an automated account's alters,
$\tilde\rho$ its median, $\tilde D$ the median degree over automated accounts having
at least one tie of that relation. The last two columns evaluate the \emph{exact}
criterion of Theorem~\ref{thm:error} at $r = 2.32$ (Section~\ref{sec:r-estimate}),
not the noise-dominated window~\eqref{eq:window}: $\rho_{\mathrm{cross}}$ is the
crossover and the final column is
$\mathrm{SNR}_{\mathrm{agg}}/\mathrm{SNR}_{\mathrm{raw}}$ at the measured $\hat\rho$,
so values below $1$ mean the relation is worse than using attributes alone. The
penultimate column evaluates that ratio with the model-implied $\eta = c\rho$; the final
column substitutes the \emph{measured} $\hat\eta$, which on this population is far
smaller because automated accounts have much lower degree
(Section~\ref{sec:balance-check}). The verdict is unchanged either way; the magnitude is
not.}
\label{tab:real}
\resizebox{\columnwidth}{!}{%
\begin{tabular}{lrrrrrrrr}
\toprule
Relation & ties & $\hat\rho$ & $\tilde\rho$ & $\hat\eta$ & $\tilde D$
 & $\rho_{\mathrm{cross}}$
 & $\frac{\mathrm{SNR}_{\mathrm{agg}}}{\mathrm{SNR}_{\mathrm{raw}}}$
 & \,''\,($\hat\eta$) \\
\midrule
\multicolumn{9}{@{}l}{\emph{social layer}}\\
following   & 231{,}829 & 0.969 & 1.00 & 0.041 & 4  & 0.252 & 0.51 & 0.02 \\
friendship  & 327{,}035 & 0.989 & 1.00 & 0.033 & 8  & 0.372 & 0.79 & 0.06 \\
mention     & 107{,}794 & 0.994 & 1.00 & 0.022 & 4  & 0.252 & 0.57 & 0.03 \\
quote       & 20{,}536  & 0.999 & 1.00 & 0.014 & 2  & 0.121 & 0.42 & 0.02 \\
reply       & 20{,}700  & 0.991 & 1.00 & 0.009 & 1  & ---   & 0.28 & 0.00 \\
\;\;union   & 331{,}251 & 0.989 & 1.00 & 0.032 & 8  & 0.372 & 0.79 & 0.06 \\
\midrule
\multicolumn{9}{@{}l}{\emph{content-coordination layer}}\\
shared URL     & 263{,}800 & 0.595 & 0.62 & 0.195 & 12 & 0.432 & 0.44 & 0.51 \\
shared hashtag & 300{,}000 & 0.627 & 0.65 & 0.279 & 53 & 0.585 & 0.71 & 0.47 \\
\bottomrule
\end{tabular}%
}
\end{table}

\subsection{Estimating the attribute separation}
\label{sec:r-estimate}

The criterion depends on $r = \|\Delta\|/\sigma$, and this quantity is easy to
overstate. The obvious plug-in---$\|\Delta\|$ from the class-mean difference and
$\sigma$ from the mean per-dimension variance---gives $r = 13.0$ on MGTAB's $788$
attributes. That figure is not usable: it implies an attribute-only error of
$\Phi(-6.5) \approx 4\times10^{-11}$, i.e.\ that the attributes classify the
population essentially perfectly and the question of whether the network helps does
not arise. A held-out linear discriminant on the same attributes attains a balanced
error of $0.123$. The discrepancy is the isotropy assumption: the per-dimension
standard deviations span a factor of $28$, so a single pooled $\sigma$ is not
meaningful, while $\|\Delta\|^2$ accumulates small differences over many correlated
coordinates.

We therefore calibrate $r$ from the achievable error rather than from the moments,
taking $r = -2\Phi^{-1}(0.123) = 2.32$, and we use that value throughout
Table~\ref{tab:real}. Resampling the split moves $r$ materially---over twelve random splits we obtain $r = 2.24 \pm 0.07$, a range of $0.22$---so we report the mean rather than a single split. The verdicts are insensitive to it: re-solving the exact criterion across that whole range leaves every relation harmful, the largest SNR ratio moving only between $0.79$ and $0.82$. This matters: at $r = 13$ the mixture term
$\rho(1-\rho)\|\Delta\|^2$ reaches $1.67\sigma^2$ and the noise-dominated
form~\eqref{eq:window} is invalid, whereas at $r = 2.32$ it is $0.05\sigma^2$ and the
approximation is defensible. Any application of these thresholds should report $r$
and how it was obtained.

\paragraph{On this population, every relation is one the network harms.}
The last column of Table~\ref{tab:real} is below $1$ for all seven recorded relations, and for their social union: under
the exact criterion, neighbour averaging on MGTAB is worse than using attributes alone
on \emph{every} one of the seven recorded relations. The size of the shortfall depends
on which $\eta$ is used: with the model-implied $\eta = c\rho$ the factors run from
$1.3$ to $3.6$, whereas substituting the measured $\hat\eta$---which is the honest
choice on a population whose classes differ in degree by a factor of five---they run from
$2$ on the content relations to more than an order of magnitude on the social ones. We
report both, because the gap between them is itself the degree effect of
Section~\ref{sec:balance-check} and not a modelling detail to be hidden. We report this
as the principal empirical finding, and we note that an earlier version of this
analysis reached the opposite conclusion for the social relations by applying the
noise-dominated window~\eqref{eq:window} at $r = 13$; the ``informative again with
reversed sign'' regime that produced is an artefact of using an $r \to 0$
approximation at large $r$, and it does not survive the exact criterion. Where the
harmful interval does have an upper edge at $r = 2.32$---shared URL $(0.432, 0.995)$
and shared hashtag $(0.585, 0.868)$---the measured $\hat\rho$ falls inside it.

\paragraph{The layers still differ, in degree of harm and in mechanism.}
What survives, and is not an artefact, is the contrast between the two layers.
On every social relation the automated accounts are almost perfectly camouflaged:
$\tilde\rho = 1.00$, so the median automated account has \emph{no} automated alter,
and automated--automated ties number $212$ in the following-and-friendship union
against $40{,}263$ automated--genuine ties. On the content relations the same accounts
behave in the opposite way: $\hat\rho$ falls to $0.60$--$0.63$ and
automated--automated ties rise to $15{,}963$ (shared URL) and $28{,}203$ (shared
hashtag). These accounts avoid one another where ties are visible and converge where
content is shared. The ratio $\hat\rho_{\text{social}}/\hat\rho_{\text{content}}$ is
$1.68$, and the resulting harm differs correspondingly: the SNR ratio ranges from
$0.79$ on friendship to $0.44$ on shared URLs.

\paragraph{What attribute-based tie scoring achieves.}
Section~\ref{sec:achievable} bounds the tolerance attainable by a threshold rule on
attribute discrepancy; we report the values here, calibrating $r$ on these networks the
same way as on MGTAB, from the balanced error of a held-out linear discriminant. That
matters, because the moment-based plug-in disagrees with it in \emph{both} directions:
on YelpChi it gives $1.17$ against a held-out $1.05$, and on Amazon $2.04$ against
$2.65$. Reporting a single method for all three datasets is the only defensible choice.

On YelpChi \texttt{homo} ($c=0.170$, $\tilde D=158$, $d=32$) the optimum moves the
tolerance from $0.7867$ to $0.7870$; on Amazon \texttt{homo} ($c=0.074$, $\tilde D=145$,
$d=25$) from $0.8539$ to $0.8808$. Both remain below the measured $\hat\rho$ of $0.805$
and $0.897$, so attribute-discrepancy scoring does not restore the inference on either
network---but we note the margins are not large, $0.018$ and $0.016$ respectively, and
that the Amazon margin halves under the corrected calibration. A modestly better score
would close it. The optimum is interior: it falls to zero as $\bar e_w \to 1$, because a
threshold rule that discards nearly every tie leaves nothing to average, and it is
stable under enlarging the search domain, which the scale-invariant soft-weight model
of~\eqref{eq:deff} is not.

These two tolerances are computed in the noise-dominated regime, unlike the MGTAB
figures in Table~\ref{tab:real}, which use the exact criterion. At these networks'
$\hat\rho$ the mixture term is $0.22\sigma^2$ (YelpChi) and $0.65\sigma^2$ (Amazon), so
the approximation is loose; the exact criterion gives slightly lower tolerances
($0.780$ and $0.840$) and therefore strengthens rather than weakens the conclusion. We
report the noise-dominated values because they are the ones the closed form yields, and
flag the discrepancy rather than let it pass silently.

\paragraph{Multiplexity, not density, decides usability.} These relations are recorded
on the \emph{same} $10{,}199$ accounts, and both $\hat\rho$ and the crossover differ
substantially between them. A procedure that pools relations into a single adjacency
is therefore averaging quantities with different thresholds, which is the concrete
form of the concern raised by V\"or\"os and Snijders~\citeyearpar{voros2017cluster} and by
Snijders et al.~\citeyearpar{snijders2013model}: the quantity is a property of a relation,
not of a population.

\paragraph{The degree-corrected identity is exact, once the estimators match.}
\label{sec:balance-check}
On the social union, the identity in its degree-corrected form
$\eta = c\rho\,(\bar d_B/\bar d_H)$ predicts $0.0647508$ against a measured
$0.0647508$: agreement to machine precision. This is not a coincidence but a
consequence of what~\eqref{eq:balance} is---an accounting identity over edge
endpoints---and it holds on \emph{any} graph provided $\rho$, $\eta$ and the mean
degrees are all estimated as ratios of sums, matching the sums the derivation counts.
The uncorrected form $\eta = c\rho$ predicts $0.365$, so the degree factor
$\bar d_B/\bar d_H = 0.177$ is doing essential work here: automated accounts have mean
degree $14.8$ against $83.5$ for genuine ones.

We flag the estimator sensitivity because it caught us out. Computing $\hat\eta$ as a
\emph{mean of per-node ratios}, while predicting it from a \emph{ratio of means},
yields $0.032$ against a predicted $0.067$ and the appearance of a factor-of-two
failure. The discrepancy is entirely an artefact of mixing the two estimators, com\-pounded
by averaging degrees over connected nodes only. Since a mean of ratios weights
low-degree actors equally with hubs, the two estimators diverge exactly in proportion
to within-class degree skew, which on this population is severe. The practical
guidance is that~\eqref{eq:balance} is a constraint an analyst can \emph{check} on
their own data, and a failed check indicates an estimator mismatch or a violated
regularity assumption rather than a violated identity.

\paragraph{Caveat.}
The window is derived for a CSBM with regular degrees, isotropic Gaussian features,
and a single relation. Real graphs violate all three: degrees are heavy-tailed,
features are neither Gaussian nor isotropic, and the relations interact. The
numbers above should be read as locating these graphs approximately, not as a
certified verdict, and the agreement of $\eta$ with $c\rho$ on YelpChi is the one
quantity here that constitutes a genuine test of the theory rather than an
application of it. Measuring $\rho$ on TwiBot remains the natural next step and
requires only data access. The estimator itself is a direct count, and on synthetic
graphs with a planted camouflage ratio it recovers that ratio exactly and reproduces
$\eta = c\rho$ to machine precision under regular degrees, so the measurement is
limited by data availability rather than by method.

\end{document}
